# Knowladge Test Project

It's a basic project for getting started with .net and react + redux

## Add migration

On the terminal  run this command

```
dotnet ef  migrations add "Initial"  -s src/Api
```
